// Suhail Al-aboud - 10675@holbertonstudents.com

// Define a Class named ClassRoom
export default class ClassRoom {
  // Constructor that accepts one argument: maxStudentsSize
  constructor(maxStudentsSize) {
    // Assign the value to the private property _maxStudentsSize
    this._maxStudentsSize = maxStudentsSize;
  }
}
